# Generate library outside

Requirements:
* To build this project, `$ANDROID_NDK` needs to be set on the enviroment to point to the location of NDK. NDK revision must be at max r18b.

This project only compiles static library.